services:
  backend:
    volumes:
      - static:/static
      - database:/data
    env_file:
      - .env
    environment:
      DB_PATH: /data/db.sqlite3
      STATIC_ROOT: /static
    build:
      context: .
    ports:
      - '8000:8000'
    privileged: true
  scanner:
    volumes:
      - static:/static
      - database:/data
    env_file:
      - .env
    environment:
      DB_PATH: /data/db.sqlite3
      STATIC_ROOT: /static
    build:
      context: .
    command: ["python", "manage.py", "run_scheduler"]
    privileged: true
    depends_on:
      - backend
  frontend:
    build:
      context: ./frontend
    ports:
      - '8080:80'
    depends_on:
      - backend

volumes:
  static:
  database:
